[pytest]
markers =
    module: module-level regression tests
    unit: isolated function tests
    integration: inter-module interaction tests
    snapshot: full-state regression tests
    edgecase: uncommon edge condition validation tests
testpaths = tests
pythonpath = .
